# Go

Go 언어 동작, 표준 라이브러리, Go 생태계 도구나 라이브러리에 직접 연결된 문서를 둡니다. 특정 문제의 원인이 Go 타입 시스템, 직렬화 규칙, 런타임/라이브러리 특성에 있을 때 이 디렉토리에서 관리합니다.

## 문서 목록

- [Go에서 JSON marshal한 `[]byte`가 DB에 base64처럼 저장되는 이유](./go-json-byte-base64-in-json-column.md)
- [Go에서 `fasthttp`를 어떻게 이해하면 좋을까](./go-fasthttp-understanding.md)

## 분류 기준

- Go 언어의 기본 동작이나 표준 라이브러리 규칙이 핵심이면 이 디렉토리에 둡니다.
- Go에서 자주 쓰는 라이브러리라도 주제가 인프라 운영, 메시징 의미론, 아키텍처 일반론이면 해당 범주를 우선합니다.
